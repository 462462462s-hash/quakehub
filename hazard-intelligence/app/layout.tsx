import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// 1. Updated this block with your high-ranking SEO Keywords and descriptions
export const metadata: Metadata = {
  title: "Earthwatch | Live Global Earthquake Tracker",
  description: "Track global seismic activity and recent earthquakes in real-time with our interactive map layouts and detailed safety alerts.",
  keywords: ["earthquake tracker", "live seismic activity", "earthwatch", "earthquake map", "seismic data"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}